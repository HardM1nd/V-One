# Быстрый старт деплоя на Dokploy

## Шаг 1: Подготовка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```bash
DJANGO_SECRET_KEY=ваш-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=ваш-домен.com,api.ваш-домен.com
USE_SQLITE=False
DB_NAME=имя_базы
DB_USER=пользователь_базы
DB_PASSWORD=пароль_базы
DB_HOST=хост_базы
DB_PORT=5432
REACT_APP_API_URL=https://api.ваш-домен.com
```

## Шаг 2: Настройка в Dokploy

### Вариант A: Docker Compose (Рекомендуется)

1. В Dokploy создайте новое приложение
2. Выберите тип: **Docker Compose**
3. Укажите путь: `docker-compose.prod.yml`
4. Добавьте все переменные из `.env` в настройках приложения
5. Нажмите "Deploy"

### Вариант B: Отдельные сервисы

#### Backend:
- Тип: **Dockerfile**
- Dockerfile: `backend/Dockerfile.prod`
- Контекст: `backend/`
- Порт: `8000`
- Добавьте переменные окружения из `.env`

#### Frontend:
- Тип: **Dockerfile**
- Dockerfile: `frontend/Dockerfile.prod`
- Контекст: `frontend/`
- Build аргументы: `REACT_APP_API_URL=https://api.ваш-домен.com`
- Порт: `80`

## Шаг 3: Настройка доменов

1. Назначьте домен для backend (например, `api.ваш-домен.com`)
2. Назначьте домен для frontend (например, `ваш-домен.com`)
3. Включите SSL для обоих доменов

## Шаг 4: Обновление CORS

После настройки доменов обновите `backend/core/settings.py`:

```python
CORS_ALLOWED_ORIGINS = [
    "https://ваш-домен.com",
]
```

И пересоберите backend контейнер.

## Готово! 🎉

Ваше приложение должно быть доступно по указанным доменам.

Для подробной документации см. `DOKPLOY.md`

